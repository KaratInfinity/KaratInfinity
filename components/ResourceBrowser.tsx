"use client";
import {useState} from 'react';
import {resources} from '@/lib/resources';
import {ResourceCards} from './ResourceCards';
export function ResourceBrowser(){const [q,setQ]=useState(''),[category,setCategory]=useState('All');const filtered=resources.filter(r=>(category==='All'||r.category===category)&&(r.title+' '+r.summary).toLowerCase().includes(q.toLowerCase()));return <><div className="filter-bar"><div className="filter-pills" aria-label="Resource categories">{['All','Guide','Overview','Ecosystem'].map(c=><button key={c} aria-pressed={category===c} className={category===c?'selected':''} onClick={()=>setCategory(c)}>{c}</button>)}</div><label className="filter-search"><span className="sr-only">Search resources</span><input type="search" placeholder="Search resources…" value={q} onChange={e=>setQ(e.target.value)}/></label></div><p className="result-count" aria-live="polite">{filtered.length} resources</p>{filtered.length?<ResourceCards items={filtered}/>:<div className="empty-state"><h3>No matching resources</h3><p>Try another search or category.</p><button className="secondary-button" onClick={()=>{setQ('');setCategory('All');}}>Clear filters</button></div>}</>;}
